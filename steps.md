## DONE:
[x] Create new project folder for API.
[x] Create an initial JS file (`app.js` / `server.js`).
[x] Initialize project with NPM (`npm init --y`).
[x] Install dependencies (`npm i express`).
[x] Start creating your server.
[x] Add `routes` folder and files if necessary (`students.js`).
[x] Create an in-memory "database" array (`data/students.js`).
[x] Add endpoints to routes.
[x] Add routes to express app.
[x] Test routes (`npm start`).
[x] Create `Dockerfile`.
[x] Build Docker image (`docker build -t <name> .`).
[x] Run Docker container (`docker run -d -p 3000:3000 <name>`).
[x] Push to DockerHub (`docker push`).

## TODO:
[] Add POST endpoint to `routes/students.js`
[] Add PUT endpoint to `routes/students.js`
[] Add DELETE endpoint to `routes/students.js`
[] Test the endpoints work with Postman.
[] Rebuild the Docker image (`docker build -t <name> .`).
[] Rerun the container from the newly built image (`docker run -d -p 3000:3000 <name>`).
[] Push the updated image to DockerHub (`docker push`).